{
  "name": "veil",
  "version": "0.1.0-beta",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "version:sync": "node scripts/sync-version.mjs",
    "test": "node scripts/run-tests.mjs",
    "sidecar": "npm run start --prefix full/sidecar",
    "release:assets": "node scripts/prepare-release-compose.mjs && node scripts/prepare-release-assets.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
