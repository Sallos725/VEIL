# VEIL Full Plugin

RisuAI-facing JavaScript plugin (`//@name veil_full`, API 3.0, version 0.0.1).

Import [veil-full.js](veil-full.js) via RisuAI Plugin Settings. Optional argument `sidecar_url` (default `http://127.0.0.1:6010`).

Start the sidecar: see [../sidecar/README.md](../sidecar/README.md).
